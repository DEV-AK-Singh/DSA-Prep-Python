import numpy as np

def main():
    arr = [1, 2, 3, 4, 5]
    for i in range(len(arr)):
        print(arr[i])
    print(np.size(arr))  # This will print the size of the array  

if __name__ == "__main__":
    main()        